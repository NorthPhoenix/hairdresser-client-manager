# Use current stable APIs by default

This is a new application, so implementation should use current stable package names, APIs, and framework patterns by default.

Legacy, compatibility, deprecated, or migration-only APIs should not be used unless there is a demonstrated blocker with the current API. If a legacy API is unavoidable, document the reason, scope, and removal path in the same change.
