# Web app for Profile Shares only

In v1, the Next.js web app is only for client-facing Profile Share pages, while Stylist client management happens in the Expo app. This keeps the web surface focused on browser-accessible shared links instead of duplicating the mobile management workflow.
