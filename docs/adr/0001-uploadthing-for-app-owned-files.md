# UploadThing for app-owned files

All app-owned binary files use UploadThing in v1, including Appointment Photos and persisted Share Images. This keeps uploads, file references, access behavior, and cleanup on one storage path instead of splitting photo and generated-image storage across multiple providers.
