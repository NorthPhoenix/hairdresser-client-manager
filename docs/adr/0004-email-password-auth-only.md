# Email/password auth only

In v1, Stylist authentication uses Clerk-managed email/password only. Google Calendar Integration remains a separate optional connection, so signing in with Google is not required and is not offered as an auth method.
