{
  "name": "hairdresser-client-manager",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=26.3.0"
  },
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "test": "pnpm lint && pnpm build"
  },
  "devDependencies": {
    "turbo": "latest",
    "typescript": "latest"
  }
}
